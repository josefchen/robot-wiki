# Build output and caches.
.next/
.next-probe/
out/
dist/
node_modules/
playwright-report/
test-results/
pagefind/
public/pagefind/
public/search-index.json
tsconfig.tsbuildinfo

# Lockfile: npm owns its formatting.
package-lock.json

# Vendored third-party runtime assets (Draco decoder for GLTFLoader), kept
# byte-identical to upstream. Same set ESLint ignores.
public/draco/

# Published editorial content. MDX bodies are prose with inline components
# (<Cite />, <Term />) whose position relative to punctuation is load-bearing
# for lib/rehype-cite-punctuation.mjs, and the no-slop lint
# (scripts/lint-no-slop.ts) reads the authored lines. Prettier reflows
# paragraphs and rewrites quotes inside MDX, so authored prose stays out of
# its scope; the TSX components those articles import are formatted.
content/

# Read-only research trail behind the content: verbatim reports, never
# reformatted. Same set ESLint ignores.
research/

# Generated by next dev/build.
next-env.d.ts
